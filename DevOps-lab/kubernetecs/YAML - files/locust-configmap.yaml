apiVersion: v1
kind: ConfigMap
metadata:
  name: locustfile
data:
  locustfile.py: |
    from locust import HttpUser, between, task

    class MyUser(HttpUser):
        wait_time = between(1, 2)

        @task
        def index(self):
            self.client.get("/")

